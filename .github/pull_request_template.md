## Summary

<!-- Why, not a diff recap. -->

## Fix or hack

<!-- Required for any change that makes something work. A *fix* is at the place
that owns the behaviour; anything else is a *hack*, including one that is small,
local, or unavoidable because the real fix lives in another repo. If it is a
hack, link the issue against the owner. -->

## Test plan

- [ ] `go build ./... && go vet ./... && go test ./... && go test -race ./...`

## Not verified

<!-- What you could not exercise. This repo has no CI, so nothing else will say
it. "Nothing" is a fine answer when it is true. -->
